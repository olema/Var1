@page
@model Var1.Pages.Users.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.WUser[0].FirstName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.WUser[0].LastName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.WUser[0].WDoljn.WDoljnID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.WUser[0].Passw)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.WUser) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FirstName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LastName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.WDoljn.DoljnName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Passw)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.WUserID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.WUserID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.WUserID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
