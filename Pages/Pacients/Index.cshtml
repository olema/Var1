@page
@model Var1.Pages.Pacients.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Пациенты</h1>

<p>
    <a asp-page="create">Создать нового</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Pacient[0].FirstName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Pacient[0].LastName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Pacient[0].MedPol)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Pacient) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FirstName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LastName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MedPol)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.PacientID">Редакт</a> |
                <a asp-page="./Details" asp-route-id="@item.PacientID">Подробно</a> |
                <a asp-page="./Delete" asp-route-id="@item.PacientID">Удалить</a>
            </td>
        </tr>
}
    </tbody>
</table>
